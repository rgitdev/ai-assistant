import { CreateMemoryCommand } from "./CreateMemoryCommand";

/**
 * Command data structure for creating conversation summary memories.
 * Simple data record with no dependencies or logic.
 */
export type CreateConversationMemoryCommand = CreateMemoryCommand;
